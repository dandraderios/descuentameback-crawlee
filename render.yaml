services:
  - type: web
    name: descuentame-crawlee
    env: docker
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    healthCheckPath: /health/check
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: PRICE_CHECKER_TOKEN
        sync: false
